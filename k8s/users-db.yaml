apiVersion: v1
kind: Service
metadata:
  name: users-db
  labels:
    app: users-db
spec:
  ports:
    - port: 5432
      targetPort: 5432
      name: db
  selector:
    app: users-db
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-db
  labels:
    app: users-db
spec:
  selector:
    matchLabels:
      app: users-db
  template:
    metadata:
      labels:
        app: users-db
    spec:
      containers:
      - name: db
        image: users-db
        ports:
        - containerPort: 5432
          name: db
---
apiVersion: v1
kind: Service
metadata:
  name: users-db-external
spec:
  type: LoadBalancer
  selector:
    app: users-db
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
